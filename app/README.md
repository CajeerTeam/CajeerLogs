# app/

Каталог оставлен как совместимый слой для старой структуры CajeerLogs. Новая архитектура использует `core/` как backend core, но `app/bootstrap.php` позволяет постепенно переносить старые скрипты и интеграции.
